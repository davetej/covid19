import unittest
import os
import sys
from airflow.models import DagBag

sys.path.insert(0, os.path.abspath(os.path.dirname(__file__)))

import validate_dags

class TestValidateDags(unittest.TestCase):

    def setUp(self):
        self.dag_bag = DagBag()

    def test_dag_with_tag_present(self):
        self.assertTrue(validate_dags.check_dag_tag('A'))

    def test_dag_with_tag_not_present(self):
        self.assertFalse(validate_dags.check_dag_tag('NonExistentTag'))

if __name__ == '__main__':
    unittest.main()
